import { Options } from 'vue-class-component';
import Base from '../../service/Base'

/* 静态页面 */
@Options({
  components: {},
})
export default class MHtml extends Base {

  /* 创建成功 */
  public created(): void {

  }

  /* 创建完成 */
  public mounted(): void {
  }

}