import { Options } from 'vue-class-component';
import Base from '../../service/Base'

/* 系统角色 */
@Options({
  components: {},
})
export default class SysRole extends Base {

  /* 创建成功 */
  public created(): void {
    
  }

  /* 创建完成 */
  public mounted(): void {
  }
  activated(): void {
  }

}