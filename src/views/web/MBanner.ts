import { Options } from 'vue-class-component';
import Base from '../../service/Base'

/* 轮播图 */
@Options({
  components: {},
})
export default class MBanner extends Base {

  /* 创建成功 */
  public created(): void {

  }

  /* 创建完成 */
  public mounted(): void {
  }

}