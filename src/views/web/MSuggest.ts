import { Options } from 'vue-class-component';
import Base from '../../service/Base'

/* 意见反馈 */
@Options({
  components: {},
})
export default class MSuggest extends Base {

  /* 创建成功 */
  public created(): void {

  }

  /* 创建完成 */
  public mounted(): void {
  }

}